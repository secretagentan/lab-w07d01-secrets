<h1>😯 Shhh</h1>
<p>Total secrets {{secrets.length}}</p>

{{#each secrets as |secret|}}
<div class="secret">
  <div class="message">{{secret.message}}</div>
  <button class="like-btn" data-id={{secret._id}}>❤️</button>
  <span class="likes">{{secret.likes}}</span>
</div>
{{/each}}
